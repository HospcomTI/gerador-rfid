// type do tb_map (coluna `type`)
export enum TipoMap {
  ITEM = "item",
  SERIE = "serie",
  LOTE = "lote",
}

// coluna TIPO do arquivo comissionador
export enum TipoComissionador {
  SERIE = 1,
  LOTE = 2,
}
